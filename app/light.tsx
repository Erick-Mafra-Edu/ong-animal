import { View } from 'react-native';
import LightTheme from '../src/app/screens/LightTheme';

export default function LightThemeScreen() {
  return (
    <View className="flex-1">
      <LightTheme />
    </View>
  );
}
